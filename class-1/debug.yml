- hosts: vm1
  become: yes
  tasks:
  - name: Install tree
    ansible.builtin.apt:
      name: tree
    register: kaizen

  - name: Debug #"show me more messages to get more context"
    debug: msg="{{ kaizen }}"